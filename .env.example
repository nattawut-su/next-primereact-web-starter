# URL ของ backend server (เช่น UAT / QA server) 
# ใส่ NEXT_PUBLIC_ เท่ากับ environment variable ที่ต้องการให้ client-side code เข้าถึงได้
NEXT_PUBLIC_API_BASE_URL=https://integration-api.company.example